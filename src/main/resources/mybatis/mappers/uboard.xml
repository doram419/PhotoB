<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	

<mapper namespace="uboard">
	<insert id="insert" parameterType="BoardVo">
	<selectKey keyProperty="UBOARD_ID" resultType="long" order="BEFORE">
			SELECT seq_board_pk.nextval from dual
	</selectKey>
	INSERT INTO UBOARD(UBOARD_ID, TITLE, CONTENT, USER_ID)
VALUES(SEQ_UBOARD_PK.nextval, #{TITLE}, #{CONTENT},#{USER_ID})
	</insert>
</mapper>