<!-- <?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="himedia.photobook.repositories.UsersRepository">

  <select id="findByEmail" resultType="UsersVo">
    SELECT * FROM USERS
    WHERE EMAIL = #{email}
  </select>
  
  <insert id="save">
    INSERT INTO USERS (USER_ID, EMAIL, PASSWARD, USER_NAME, ADDRESS, PHONE_NUMBER, ROLE)  
    VALUES (#{user_id}, #{email}, #{password}, #{user_name}, #{address}, #{phone_number})
  </insert>

</mapper> -->