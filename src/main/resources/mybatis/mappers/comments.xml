<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
   
   <mapper namespace="comments">
   	<insert id="insert" parameterType="CommentsVo">
		<selectKey keyProperty="commentId" resultType="long"
			order="BEFORE">
			SELECT SEQ_COMMENTS_PK.nextval from dual
		</selectKey>
		INSERT INTO COMMENTS(COMMENT_ID, USER_NAME, CONTENT, COM_DATE)
		VALUES
		(SEQ_COMMENTS_PK.nextval, #{userName}, #{content}, #{comDate})
	</insert>
   </mapper>