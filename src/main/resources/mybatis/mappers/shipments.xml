<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="shipments">
	<select id="selectStatusByOrderID" parameterType="String" resultType="String">
		SELECT 
			SHIPMENT_STATUS
		FROM
		 	SHIPMENTS
		WHERE
			ORDER_ID = #{value}
	</select>
	
	<select id="selectShipmentInfoByOrderID" parameterType="String" resultType="ShipmentsVo">
		SELECT 
			SHIPMENT_ID as shipmentId,
			ORDER_ID as orderId,
			SHIPMENT_DATE as shipmentDate,
			SHIPMENT_STATUS as shipmentStatus
		FROM
		 	SHIPMENTS
		WHERE
			ORDER_ID = #{value}
	</select>
</mapper>