<mapper namespace="himedia.photobook.repositories.UsersRepository">
    <select id="findByEmail" resultType="himedia.photobook.repositories.vo.UsersVo">
        SELECT * FROM USERS
        WHERE EMAIL = #{email}
    </select>

    <insert id="save">
        INSERT INTO USERS (USER_ID, EMAIL, PASSWORD, USER_NAME, CREATED_AT, UPDATED_AT)
        VALUES (#{userId}, #{email}, #{password}, #{userName}, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
    </insert>
</mapper>