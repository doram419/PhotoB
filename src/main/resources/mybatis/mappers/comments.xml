<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
   
<mapper namespace="comments">
	<select id="getCommentsByBoardId" parameterType="Long" resultType="CommentsVo">
		SELECT COMMENT_ID as commentId, BOARD_ID as boardId, USER_NAME as userName, CONTENT, COM_DATE as comDate
		FROM COMMENTS 
		WHERE BOARD_ID = #{boardId}
	</select>	

   <insert id="insert" parameterType="CommentsVo" useGeneratedKeys="true" keyProperty="commentId">
		INSERT INTO COMMENTS(BOARD_ID, USER_NAME, CONTENT, COM_DATE)
		VALUES
		(#{boardId}, #{userName}, #{content}, NOW())
	</insert>
</mapper>