<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="photo">
	<select id="selectCountByOrderId" parameterType="String" resultType="int">
		SELECT COUNT(*)
		FROM PHOTO 
		WHERE ORDER_ID = #{value}
	</select>
	
	<insert id="insert" parameterType="PhotoVo" useGeneratedKeys="true" keyProperty="photoId">
		INSERT INTO PHOTO(ORDER_ID, PHOTO_PATH, PHOTO_SEQ_NO)
		VALUES(#{orderId}, #{photoPath}, #{photoSeqNo})
	</insert>
</mapper>