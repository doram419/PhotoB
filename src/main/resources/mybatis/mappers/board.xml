<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	

<mapper namespace="board">
	<select id="selectAll" resultType="BoardVo">
		SELECT BOARD_ID AS boardId, TITLE, CONTENT,REG_DATE as regDate, USER_ID as userId
		FROM BOARD
	</select>


	<insert id="insert" parameterType="BoardVo">
	<selectKey keyProperty="boardId" resultType="long" order="BEFORE">
		SELECT seq_board_pk.nextval from dual
	</selectKey>
		INSERT INTO BOARD(BOARD_ID, TITLE, CONTENT, USER_ID)
		VALUES (SEQ_BOARD_PK.nextval, #{title}, #{content}, #{userId})
	</insert>
</mapper>